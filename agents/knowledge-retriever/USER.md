# USER.md

- The user operates an extensive Personal Knowledge Management (PKM) vault in Obsidian (`~/pkm`), partitioned into personal notes (`vault/`), tasks (`ticktick/` and markdown tasks), and agent-synthesized wiki pages (`wiki/`).
- The user relies on hybrid search (dense semantic embeddings + sparse BM25) and SQLite task indexes to quickly recall context across thousands of notes and tasks.
- The user values precision, zero hallucinations, clean markdown formatting, and explicit citations linking back to original files and sections.
